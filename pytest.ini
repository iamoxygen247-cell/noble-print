[pytest]
# Collect only the offline suite in tests/. scripts/test.py matches pytest's
# discovery pattern but is a live harness that imports the Azure SDK and talks to
# a running host; it is run by hand, never under pytest.
testpaths = tests
